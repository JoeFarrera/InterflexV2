package es.sysmap.interflex.model.dmc;
import oracle.jbo.server.ViewObjectImpl;
//  ---------------------------------------------------------------------
//  ---    File generated by Oracle ADF Business Components Design Time.
//  ---    Custom code may be added to this class.
//  ---    Warning: Do not modify method signatures of generated methods.
//  ---------------------------------------------------------------------

public class SgavordtranMLDAgrupImpl extends ViewObjectImpl implements es.sysmap.interflex.model.dmc.common.SgavordtranMLDAgrup 
{
  /**
   * 
   *  This is the default constructor (do not remove)
   */
  public SgavordtranMLDAgrupImpl()
  {
  }
  
  public void agrupOrdtranMLD()
  {
    clearCache();
    executeQuery();
    if (getRowCount() > 1)
    {
      // Hay más de 1
      // Si el segundo es de segunda posición, no lo saca ya que no es posible
      SgavordtranMLDAgrupRowImpl firstRow = (SgavordtranMLDAgrupRowImpl)first();
      SgavordtranMLDAgrupRowImpl secondRow = (SgavordtranMLDAgrupRowImpl)next();
      if (secondRow.getPosubipos().intValue() == 1)
      {
          firstRow.setSeqmld(new oracle.jbo.domain.Number(1));
          firstRow.setEstado("N");
          secondRow.setSeqmld(new oracle.jbo.domain.Number(2));
          secondRow.setEstado("N");
      }
    }
  }
}